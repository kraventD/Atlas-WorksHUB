general {
    lock_cmd = false
    unlock_cmd = false
}

listener {
    timeout = 600
    on-timeout = notify-send "Idle" "System will suspend soon..."
}

listener {
    timeout = 900
    on-timeout = systemctl suspend
}
